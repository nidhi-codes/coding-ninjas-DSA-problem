#include <bits/stdc++.h> 
string replaceSpaces(string &str){
	// Write your code here.

	string ans = "";
	int len = str.length();

	for(int i=0; i<len; i++){
		if(str[i] == ' '){
			ans += "@40";
		}
		else{
			ans += str[i];
		}
	}

	return ans;

}
